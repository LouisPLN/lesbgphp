{% extends 'base.html.twig' %}

{% block title %}Adoptoutou | {{ pet.name }} 🐶{% endblock %}

{% block body %}

<div class="container">
    <main>

    <h2>Profil Adoptoutou 🐶</h2>

        <div class="cardPet">
            <div class="titlePet"><small>Nom :</small>
                <h3>{{ pet.name }}</h3>
            </div>
            <p> Référence : {{ pet.id }} </p>
            <p> Type : {{ pet.type }} </p>
            <p> Race : {{ pet.race }} </p>
            <p> Age : {{ pet.age }} </p>
            <p> Poids : {{ pet.weight }} </p>
            <p> Couleur : {{ pet.color }} </p>
            <p> Adopté :
                {% if (pet.state == 1) %}
                Oui
                {% else %}
                Non
            </p>
            {% endif %}
            <img class="pet-img" src="{{ pet.urlImg }}" alt="Image Pet">
        

        {% if (pet.state == 1) %}
        
        {% else %}
        <a class="detail"> <button class="button" id="btn_adopt">ADOPTEZ LE</button></a>
        </div>
        <div id="form_adoption" style="display:none;">
            <form action="{{ path('pets_adopt',{ 'id' :pet.id} )}}" method="post">
                    <label for="mail">Votre mail :</label>
                    <input class="text" type="email" id="mail" name="user_mail">

                    <input class="save" type="submit" id="valid" name="user_valid" value="ADOPTER L'ANIMAL">

            </form>
        </div>
        {% endif %}
        </p>

</div>

</main>

</div>
{% block javascripts %}

<script>
    document.getElementById("btn_adopt").onclick = function () {

        var y = document.getElementById("form_adoption");

        if (y.style.display === "none") {
            y.style.display = "block";
        } else {
            y.style.display = "none";
        }
    }
</script>

{% endblock %}

<style>
    body {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Rubik', sans-serif;
    }

    .container {
        background: #f1f1f1;
        min-height: 100vh;
        padding: 50px;
    }

    .container a {
        display: flex;
        justify-content: center;
        align-items: center;
        width: 30%;
        margin: auto;
        padding: 15px 80px;
        font-size: 15px;
        text-align: center;
        border-radius: 4px;
        text-decoration: none;
        color: #fff;
        background-color: #723647;
        transition: border-color .4s, transform .4s, box-shadow .4s, -webkit-transform .4s;
    }

    .container a:hover {
        box-shadow: 0 6px 12px -2px #723647;
        -webkit-transform: translate(0, -3px);
        -ms-transform: translate(0, -3px);
        transform: translate(0, -3px);
    }

    main {
        margin: auto;
        width: 70%;
    }

    main h2 {
        text-align: center;
        font-size: 40px;
        color: #303030;
        margin-bottom: 50px
    }

    .section {
        display: flex;
        flex-flow: row wrap;
    }

    .cardPet {
        margin: auto;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        width: 500px !important;
        max-height: 800px;
        margin-top: 30px;
        padding: 30px;
        border-radius: 4px;
        border: 2px solid #303030;
        background: #fff;
    }

    .cardPet .titlePet {
        display: flex;
        margin-bottom: 20px;
    }

    .cardPet small {
        margin-top: 5px;
        margin-right: 5px;
        font-size: 18px;
        color: grey;
    }

    .cardPet h3 {
        font-size: 25px;
        color: #303030;
    }


    .cardPet p {
        font-size: 18px;
        color: #303030;
        margin-bottom: 10px;
    }

    .cardPet img {
        width: 100%;
        border-radius: 4px;
        border: 2px solid #723647;
        margin-top: 10px;
        margin-bottom: 10px;
    }

    a.detail {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 15px 80px;
        font-size: 15px;
        text-align: center;
        border-radius: 4px;
        color: #fff;
        background-color: #723647;
        transition: border-color .4s, transform .4s, box-shadow .4s, -webkit-transform .4s;
    }

    a.detail:hover {
        box-shadow: 0 6px 12px -2px #723647;
        -webkit-transform: translate(0, -3px);
        -ms-transform: translate(0, -3px);
        transform: translate(0, -3px);
    }

    .button {
        text-align: center;
        border: none;
        background: none;
        color: #fff;
    }

    form {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
    }

    form label {
        font-size: 20px;
        margin-bottom: 20px;
        color: #303030;
        text-align: center;
    }

    form input.text {
        height: 40px;
        width: 30%;
        outline: none;
        margin-bottom: 20px;
        padding: 10px;
        border-radius: 4px;
        border: 1px solid lightgrey;
        font-size: 15px;
        transition: all 0.3s ease;
    }

    form input.save {
        display: flex;
        justify-content: center;
        align-items: center;
        outline: none;
        border: none;
        width: 30%;
        margin: auto;
        padding: 15px 80px;
        font-size: 15px;
        text-align: center;
        border-radius: 4px;
        text-decoration: none;
        color: #fff;
        background-color: #723647;
        transition: border-color .4s, transform .4s, box-shadow .4s, -webkit-transform .4s;
    }

    input.save:hover {
        box-shadow: 0 6px 12px -2px #723647;
        -webkit-transform: translate(0, -3px);
        -ms-transform: translate(0, -3px);
        transform: translate(0, -3px);
    }
</style>

{% endblock %}